spring:
  cloud:
    aws:
      secrets-manager:
        packages:
          - io.symplesims.spring.aws.secretsmanager.ripebeans

---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    aws:
      secrets-manager:
        profile: dev

